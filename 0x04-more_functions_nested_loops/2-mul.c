#include "main.h"
/**
 * mul - multiplies two integers (a and b)
 * @a: first number to be multiplied
 * @b: number to be multiplied against
 * Return: the product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
